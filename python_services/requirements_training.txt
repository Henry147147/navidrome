# Requirements for MusicBench Text-to-Audio Embedding Training
# Install with: pip install -r requirements_training.txt

# Core ML frameworks
torch>=2.0.0
torchaudio>=2.0.0
transformers>=4.35.0
sentence-transformers>=2.2.0

# LoRA fine-tuning
peft>=0.7.0

# Dataset and data processing
datasets>=2.14.0
h5py>=3.9.0
numpy>=1.24.0
scipy>=1.11.0

# Training utilities
tensorboard>=2.14.0
tqdm>=4.65.0

# Audio processing (may already be installed)
librosa>=0.10.0
soundfile>=0.12.0

# Utilities
safetensors>=0.4.0

# Optional: For better performance
# accelerate>=0.24.0  # For distributed training if needed
# deepspeed>=0.11.0   # For memory optimization if needed
